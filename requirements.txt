# ChipForge subnet - direct dependencies.
#
# Bittensor "classic" SDK line (10.x). This is the last major line that still
# ships the neuron networking layer (bt.Synapse / bt.axon / bt.dendrite) that
# chipforge/protocol.py, neurons/miner.py and neurons/validator.py build on.
# bittensor 11 removed that layer, so do NOT bump past 10.x without rewriting
# the miner/validator transport.
#
# NOTE: bittensor-cli 9.23.2 hard-pins bittensor-wallet==4.1.0, and bittensor
# 10.5.0 accepts >=4.1.0, so 4.1.0 is the newest wallet both can share.
# NOTE: do not pin scalecodec here. async-substrate-interface 2.x depends on
# cyscale, which installs under the `scalecodec` module name; pinning both
# makes them overwrite each other.
bittensor==10.5.0
bittensor-wallet==4.1.0
bittensor-cli==9.23.2
bittensor-drand==2.0.0
async-substrate-interface==2.2.1
bt-decode==0.8.0

# Application dependencies (transitives are resolved by pip; see
# requirements-lock.txt for the exact set that was tested).
torch>=2.4
aiohttp>=3.10
aiofiles>=24.1
requests>=2.32
python-dotenv>=1.0
cryptography>=43
